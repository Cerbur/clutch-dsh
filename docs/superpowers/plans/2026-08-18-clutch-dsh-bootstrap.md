# Clutch DSH Workspace Bootstrap Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立一个可持续扩展的 pnpm workspace，用统一的根目录工具、package 形状校验和 DSH patch 校验管理后续 plugin。

**Architecture:** 根目录只负责 workspace、共享 TypeScript/lint/format 配置、校验脚本和统一命令，不发布为 plugin。`packages/*` 下只有包含 `package.json` 的目录才被视为可运行 package；README/计划形式的规划目录可以保留而不会被 guard 误判。真实 Service Definition、Provider、Consumer 按命名和依赖规则在后续 capability 中创建。

**Tech Stack:** pnpm workspace、TypeScript、ESLint、Prettier、Node.js 检查脚本、Cordis patch YAML。

## Global Constraints

- 根 `package.json` 必须设置 `private: true`，根 package 不发布。
- 根 `package.json` 的项目名必须是 `clutch-dsh`。
- `pnpm-workspace.yaml` 只纳入 `packages/*`。
- Service Definition 的命名格式为 `dsh-<capability>`，目录为 `<capability>`。
- Provider 的目录格式为 `<capability>-local`，package 名称为 `dsh-<capability>-local`。
- Consumer 的目录格式为 `tool-<capability>`，package 名称为 `dsh-tool-<capability>`。
- Provider 和 Consumer 对 Service Definition 使用精确的 `workspace:*` 依赖；Service Definition 不依赖具体 Provider 或 Consumer。
- 每个可运行 plugin package 都必须包含 `package.json`、`cordis.patch.yml`、`tsconfig.json` 和 `src/index.ts`，并提供 `build`、`lint`、`typecheck`、`test` scripts。
- 每个可运行 plugin 的 patch 必须让 DSH loader 解析出对应 Service Definition 的 `dsh.bundle`。
- 没有 `package.json` 的规划目录不属于可运行 package，workspace 和 patch guard 必须跳过它。
- 本次不创建 `my-cap` 等 demo package；这些名称只允许作为 authoring 文档中的示例。
- 本次不实现真实 DSH runtime API、外部服务凭据、发布流程或 `clutch-dsh-worktree` Provider/Consumer。

---

## Current Baseline

当前仓库没有既有 runtime package，只有根/插件说明和计划文档。`packages/clutch-dsh-worktree/` 是规划入口，不包含 `package.json`，因此初始化后仍作为规划目录保留。

## Target Structure

```text
clutch-dsh/
├── package.json                       # private pnpm workspace root
├── pnpm-workspace.yaml                # packages: ["packages/*"]
├── pnpm-lock.yaml                     # generated by pnpm install
├── tsconfig.base.json                 # shared TypeScript defaults
├── eslint.config.mjs                  # shared ESLint flat config
├── prettier.config.mjs                # shared formatting rules
├── .gitignore
├── scripts/
│   ├── check-workspace.mjs            # package shape and dependency checks
│   └── validate-cordis-patches.mjs    # dsh.bundle checks
├── README.md
├── docs/
│   ├── PLUGIN_AUTHORING.md            # how to add a new capability
│   └── superpowers/
│       ├── plans/2026-08-18-clutch-dsh-bootstrap.md
│       └── specs/2026-08-18-clutch-dsh-bootstrap-design.md
└── packages/
    └── clutch-dsh-worktree/           # planning entry; no runnable package yet
        ├── AGENTS.md
        ├── README.md
        └── docs/superpowers/plans/2026-08-18-clutch-dsh-worktree.md
```

本次不会创建 `packages/my-cap/`、`packages/my-cap-local/` 或 `packages/tool-my-cap/`。

## Package Boundaries

| Package role                          | Owns                                                                  | May depend on                                | Must not own                                  |
| ------------------------------------- | --------------------------------------------------------------------- | -------------------------------------------- | --------------------------------------------- |
| Service Definition `dsh-<capability>` | capability name、公共类型、Service 接口和 loader 可识别的 bundle 身份 | DSH/Cordis 的公共类型或 runtime contract     | 本地实现、具体外部服务、Consumer 行为         |
| Provider `dsh-<capability>-local`     | 一个可运行的本地实现，以及把实现绑定到 Service Definition 的 patch    | 对应 Service Definition 和 Provider 实现依赖 | 重新定义公共 Service 类型                     |
| Consumer `dsh-tool-<capability>`      | 面向用户或上层流程的工具入口，以及对 Service 的调用                   | 对应 Service Definition 和 Consumer 实现依赖 | 直接复制 Provider 实现或绕过 Service contract |
| Root                                  | workspace、共享配置、检查脚本和统一命令                               | tooling dependencies                         | 任何具体 capability 的运行时实现              |

## Implementation Tasks

### Task 1: 建立 pnpm workspace 根目录

**Files:**

- Create: `package.json`
- Create: `pnpm-workspace.yaml`
- Create: `.gitignore`
- Generate: `pnpm-lock.yaml`

**Interfaces:**

- Consumes: 空仓库和规划文档。
- Produces: 一个 private workspace root；后续所有含 `package.json` 的 plugin 都通过 `packages/*` 被发现。

- [x] **Step 1: 创建根 package.json**

```json
{
  "name": "clutch-dsh",
  "private": true,
  "scripts": {}
}
```

- [x] **Step 2: 创建 pnpm-workspace.yaml**

```yaml
packages:
  - 'packages/*'
```

- [x] **Step 3: 创建根 .gitignore**

```gitignore
node_modules/
dist/
coverage/
.pnpm-store/
.DS_Store
*.tsbuildinfo
```

- [x] **Step 4: 生成 workspace lockfile**

Run: `pnpm install`

Expected: 命令成功并生成 `pnpm-lock.yaml`；根 package 保持 private，未产生发布产物。

- [x] **Step 5: 验证 workspace 配置**

Run: `pnpm list --depth -1`

Expected: 命令成功；`packages/clutch-dsh-worktree/` 作为规划目录存在也不导致 workspace 配置错误。

### Task 2: 添加根目录共享工具链和 package 形状检查

**Files:**

- Modify: `package.json`
- Create: `tsconfig.base.json`
- Create: `eslint.config.mjs`
- Create: `prettier.config.mjs`
- Create: `scripts/check-workspace.mjs`
- Create: `scripts/check-workspace.test.mjs`

**Interfaces:**

- Consumes: Task 1 的 pnpm workspace root。
- Produces: 根级 format、lint、typecheck、test、build、check:workspace 和 check 命令；`check-workspace.mjs` 负责发现可运行 package 的结构错误。

- [x] **Step 1: 安装共享开发依赖**

Run: `pnpm add -Dw typescript eslint @eslint/js typescript-eslint prettier yaml`

Expected: 依赖只写入根 `package.json` 的 `devDependencies`，版本记录到 `pnpm-lock.yaml`，不创建任何 demo package。

- [x] **Step 2: 创建共享 TypeScript 配置**

Create `tsconfig.base.json`:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "noEmitOnError": true,
    "skipLibCheck": true
  }
}
```

- [x] **Step 3: 创建共享 ESLint 配置**

Create `eslint.config.mjs`:

```js
import eslint from '@eslint/js';
import tseslint from 'typescript-eslint';

export default tseslint.config(
  {
    ignores: ['**/dist/**', '**/coverage/**', 'pnpm-lock.yaml'],
  },
  eslint.configs.recommended,
  ...tseslint.configs.recommended,
);
```

- [x] **Step 4: 创建共享 Prettier 配置**

Create `prettier.config.mjs`:

```js
export default {
  singleQuote: true,
  semi: true,
  trailingComma: 'all',
  printWidth: 100,
};
```

- [x] **Step 5: 添加根级统一 scripts**

将以下 scripts 合并到根 `package.json`，保留 `private: true`：

```json
{
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "check": "pnpm run check:workspace && pnpm run check:patches && pnpm run format:check && pnpm run lint && pnpm run typecheck && pnpm run test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "node --test scripts/*.test.mjs && pnpm -r --if-present run test",
    "check:workspace": "node scripts/check-workspace.mjs",
    "check:patches": "node scripts/validate-cordis-patches.mjs"
  }
}
```

- [x] **Step 6: 实现 scripts/check-workspace.mjs**

脚本只使用 `node:fs/promises`、`node:path` 和 `node:process`，并且必须：

1. 读取 `packages/` 的直接子目录；目录不存在时成功退出。
2. 对没有 `package.json` 的目录直接跳过，使规划入口不会被当作 plugin package。
3. 对存在 `package.json` 的目录解析 metadata，并要求 `build`、`lint`、`typecheck`、`test` scripts，以及 `cordis.patch.yml`、`tsconfig.json`、`src/index.ts`。
4. 按目录角色校验 package 名：`tool-<capability>` -> `dsh-tool-<capability>`；`<capability>-local` -> `dsh-<capability>-local`；其他 package 目录 -> `dsh-<directory>`。
5. 对 Provider 和 Consumer 要求对应 Service Definition 依赖值精确为 `workspace:*`。
6. 每个失败都输出 package 路径和原因，并以状态码 1 退出；无失败时输出 `workspace shape ok` 并以状态码 0 退出。

- [x] **Step 7: 运行根级工具链检查**

Run: `pnpm run format:check`

Expected: 根配置和现有规划文档格式检查成功。

Run: `pnpm run lint`

Expected: `eslint.config.mjs` 和 `scripts/check-workspace.mjs` 检查成功。

Run: `pnpm run check:workspace`

Expected: 规划目录被跳过，输出 `workspace shape ok`。

### Task 3: 添加通用 Cordis patch 校验

**Files:**

- Modify: `package.json`
- Create: `scripts/validate-cordis-patches.mjs`
- Create: `scripts/validate-cordis-patches.test.mjs`

**Interfaces:**

- Consumes: Task 2 的根 `yaml` 依赖和 package 目录规则。
- Produces: `pnpm run check:patches`；它只校验实际 package，不要求规划目录有 patch。

- [x] **Step 1: 定义 patch validator 的输入和输出**

实现 `validatePatch(packageDirectory) -> { packageName, bundleReference }`。脚本扫描 `packages/*`，跳过没有 `package.json` 的规划目录，解析每个实际 package 的 `cordis.patch.yml`。

- [x] **Step 2: 实现 bundle 推导和错误报告**

使用以下确定性规则：

```js
function expectedBundle(folderName) {
  if (folderName.startsWith('tool-')) return 'dsh-' + folderName.slice(5);
  if (folderName.endsWith('-local')) return 'dsh-' + folderName.slice(0, -6);
  return 'dsh-' + folderName;
}
```

YAML 无法解析、缺少 `dsh.bundle` 或其值不等于 `expectedBundle(folderName)` 时，报告 patch 文件路径和原因并以状态码 1 退出；无实际 package 时输出 `cordis patches ok`。

- [x] **Step 3: 运行当前仓库 patch 检查**

Run: `pnpm run check:patches`

Expected: `packages/clutch-dsh-worktree/` 被跳过，输出 `cordis patches ok`。

### Task 4: 编写 plugin authoring 文档

**Files:**

- Create: `README.md`
- Create: `docs/PLUGIN_AUTHORING.md`

**Interfaces:**

- Consumes: Tasks 1–3 的目录、命名和命令约定。
- Produces: 新成员可以按文档创建真实 capability package，并在提交前运行同一套检查；文档中的 `my-cap` 只是示例，不会生成到本仓库。

- [x] **Step 1: 创建根 README**

README 必须说明项目用途、根 package 为 private、实际 runtime units 位于 `packages/*`，并列出：

```text
pnpm install
pnpm run check
pnpm run build
pnpm run test
```

- [x] **Step 2: 创建 docs/PLUGIN_AUTHORING.md**

文档必须覆盖：

1. Service Definition：`packages/<capability>/` -> `dsh-<capability>`，只导出公共 contract。
2. Provider：`packages/<capability>-local/` -> `dsh-<capability>-local`，依赖 `dsh-<capability>: workspace:*`。
3. Consumer：`packages/tool-<capability>/` -> `dsh-tool-<capability>`，只依赖 Service Definition。
4. Patch：每个实际 package 都要有 `cordis.patch.yml`，其 `dsh.bundle` 指向 Service Definition。
5. Validation：新增 package 后运行 `pnpm install`、`pnpm run check:workspace`、`pnpm run check:patches`、`pnpm run check`。
6. Boundary rule：Service Definition 不引用 Provider/Consumer；Consumer 不直接引用 Provider。
7. 明确说明 `my-cap`、`file-cap` 仅用于解释命名，不是当前仓库要创建的 package。

- [x] **Step 3: 做文档命名演练**

只在临时目录中将 `<capability>` 替换为 `file-cap`，确认文档推导出：

```text
packages/file-cap       -> dsh-file-cap
packages/file-cap-local -> dsh-file-cap-local
packages/tool-file-cap  -> dsh-tool-file-cap
dsh.bundle              -> dsh-file-cap
```

不得把临时目录或 demo package 加入仓库。

## Final Verification

按以下顺序执行：

```bash
pnpm install --frozen-lockfile
pnpm run check:workspace
pnpm run check:patches
pnpm run format:check
pnpm run lint
pnpm run typecheck
pnpm run build
pnpm run test
pnpm run check
```

Expected result：

- lockfile 安装成功，workspace 配置有效。
- 规划目录不会被误判为可运行 package。
- 当前没有 demo package、demo dist 或 coverage 文件。
- 根级 guard、format、lint、typecheck、build、test 和 check 全部返回 0。
- 新增真实 package 后，命名、依赖和 `dsh.bundle` 规则可由同一套 guard 验证。

## Scope Boundary

本计划只完成仓库基础设施、通用 guard 和 authoring 文档。以下事项另行按 capability 立项：真实 DSH runtime API、`clutch-dsh-worktree` Service Definition/Provider/Consumer、外部 Provider 认证与网络访问、Consumer 产品交互、版本发布和 CI/CD。
